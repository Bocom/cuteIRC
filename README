This client is functional but very primitive.

It is currently using a multi-window interface,
and only very few commands are supported. Several
of the commands that actually are supported are buggy :)

Nickserv auto-identify is not yet handled automatically.
